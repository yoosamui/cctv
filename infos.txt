sudo systemctl enable systemd-networkd-wait-online.service


Best practical solution (recommended)

Add a simple wait loop before starting Python:

Option A: inline in service
ExecStartPre=/bin/bash -c 'until ping -c1 -W1 192.168.1.82; do sleep 2; done'
ExecStart=/usr/bin/python3 -u /home/pi/cctv/cctv-video-recorder.py --name %i --url ${URL}



[Unit]
After=network-online.target
Wants=network-online.target


🔥 Simple alternative (cron job)

If you want something simpler:

crontab -e
0 3 * * * sudo apt update && sudo apt -y upgrade
Runs every day at 03:00.


3. Prevent critical packages from updating

Some updates can break things:
python
ffmpeg
opencv
Freeze them:
sudo apt-mark hold python3 ffmpeg
Check:
apt-mark showhold
This is one of the most effective protections.
👉 This is one of the most effective protections.
✅ 4. Add a health check (auto-restart if camera fails)

Create a watchdog script:

nano /home/pi/cctv/check_camera.sh
#!/bin/bash

if ! nc -z 192.168.1.82 554; then
    echo "Camera down, restarting service..."
    systemctl restart cctv-video-recorder@Center.service
fi

Make executable:

chmod +x /home/pi/cctv/check_camera.sh

Add cron:

crontab -e
*/2 * * * * /home/pi/cctv/check_camera.sh





source /home/ops/PYEVO/bin/activate
pip install python-dotenv

sudo chmod 644 /etc/cctv/credentials.env


